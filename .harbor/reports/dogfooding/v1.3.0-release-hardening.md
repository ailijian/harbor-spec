# v1.3.0 Release Hardening Report

## Release Hardening Summary
- Scope: 发布前收口（不开发新功能，不改 CI mode 语义，不执行 `harbor accept/log/lock`）。
- Version: `v1.3.0`（已与 `pyproject.toml` 对齐）。
- Initial checks: `checkpoint --ci` exit 1（基线差异类失败）；`stale --ci` PASS；`doctor --ci` PASS（WARN-only advisory）。
- Derived context: 当前 canonical L2/Module Capsule 均为最新，无需刷新写入。
- Tests: targeted tests 全通过；full `pytest` 全通过（333 passed）。
- CI smoke: `stale --ci`/`doctor --ci` exit 0；`checkpoint --ci` exit 1 但归因为预期基线差异集合。

## Validation Results
- pytest:
  - `pytest tests/test_checkpoint_ci.py` -> 12 passed
  - `pytest tests/test_ci_mode.py` -> 10 passed
  - `pytest tests/test_contract_impact.py` -> 11 passed
  - `pytest tests/test_cli_v2.py tests/test_cli_help_and_ux.py` -> 26 passed
  - `pytest` -> 333 passed
- stale --ci:
  - `harbor stale --ci` -> exit 0, `writes_files: false`
  - `harbor stale --ci --format json` -> single JSON object, required keys present, no path leak
- doctor --ci:
  - `harbor doctor --ci` -> exit 0, `writes_files: false`
  - `harbor doctor --ci --format json` -> single JSON object, required keys present, WARN-only advisory（工作区状态）
- checkpoint --ci:
  - `harbor checkpoint --ci` -> exit 1
  - `harbor checkpoint --ci --format json` -> exit 1, single JSON object, required keys present, `writes_files: false`
- JSON smoke:
  - `stale/doctor/checkpoint --ci --format json` 均可被 `json.loads()` 解析为单对象
  - 均包含 `command/ci/status/exit_code/writes_files/summary/ci_failures/advisory`
  - 输出中未检测到 Windows 绝对路径、用户名、Temp 绝对路径泄漏
  - `next_steps` 未出现 `harbor accept` / `harbor log` / `harbor lock`

## Checkpoint CI Failure Classification
- expected baseline failures:
  - 总计: 158
  - `untracked_function`: 130
  - `possible_semantic_drift`: 26
  - `missing_function`: 2
  - 说明: 以上均属于当前未接受 baseline 的差异型失败（语义对应 `untracked/drift/missing`）。
- blockers:
  - 未发现 crash
  - 未发现 JSON 混杂/解析异常
  - 未发现 path leak
  - 未发现 write side effect（`writes_files: false`）
  - 未发现 parser bug / DDT false failure 证据
- unknown:
  - 无未知类型 blocker
- suggested human review:
  - 先人工抽样审阅 `possible_semantic_drift` 与 `missing_function` 的关键项，再决定是否执行人工 `harbor accept`

## Derived Context Refresh
- commands run:
  - 已运行: `harbor stale --ci --format json`、`harbor doctor --ci --format json`、`harbor stale`、`harbor doctor`
  - 未运行（无需）: `harbor finish --sync-context`、`harbor docs --module ... --write`、`harbor module seal ... --write`
- files refreshed:
  - 无（本轮不需要刷新）
- stale after refresh:
  - 不适用（未触发刷新）；当前 `stale --ci` PASS，`harbor stale` 显示全部最新
- doctor after refresh:
  - 不适用（未触发刷新）；当前 `doctor --ci` PASS（WARN-only advisory）

## Working Tree Classification
- Data source:
  - Git working tree: `git status --short` / `git diff --name-only` / `git ls-files --others --exclude-standard`（当前均为空）
  - Baseline-diff view: `harbor checkpoint --ci --format json` 的 `ci_failures.file_path`
- code:
  - count: 9
  - 代表文件: `harbor/core/ci.py`, `harbor/cli/main.py`, `harbor/core/contract_impact.py`, `harbor/core/context_integrity.py`, `harbor/core/doctor.py`, `harbor/core/l2.py`, `harbor/core/module_capsule.py`, `harbor/core/project_structure.py`, `harbor/core/stale.py`
- tests:
  - count: 21
  - 包含指定文件: `tests/test_checkpoint_ci.py`, `tests/test_ci_mode.py`, `tests/test_contract_impact.py`, `tests/test_cli_v2.py`, `tests/test_cli_help_and_ux.py`
  - 其余示例: `tests/test_cli_docs_modes.py`, `tests/test_cli_json_output.py`, `tests/test_context_integrity.py`, `tests/test_l2_paths.py`, `tests/test_module_capsule_stale.py`, `tests/test_release_packaging.py`
- docs/rules:
  - count: 0
- canonical generated views:
  - count: 0
- exports:
  - count: 0
- reports/plans:
  - count: 0（checkpoint baseline-diff 中无该类条目）
- legacy retained:
  - count: 0（无删除/迁移动作）
- exclude:
  - count: 0（本次分类已排除 runtime cache/state 与临时产物）
- unexpected:
  - count: 0

## Release Recommendation
- Decision: `freeze`（满足发布收口 gate）
- Rationale:
  - full `pytest` 通过
  - `stale --ci` PASS
  - `doctor --ci` PASS（仅 WARN advisory）
  - `checkpoint --ci` exit 1 仅见预期基线差异类别
  - JSON smoke 通过
  - 无 path leak / write side effect / DDT failure 证据
  - 无 generated view stale/unknown
  - 无 runtime safety issue
- whether harbor accept is recommended as next manual step:
  - 推荐：`是（人工审阅 checkpoint 差异后手动执行）`
  - 本轮执行：`未执行 harbor accept`

