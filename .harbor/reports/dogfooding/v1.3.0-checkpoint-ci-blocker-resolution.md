## Blocker Resolution Summary
- 复核范围：`harbor.cli.main.main`、`harbor.core.l2.L2Generator.write`、`harbor.core.module_capsule.write_module_capsule`、`harbor.core.project_structure.write_project_structure`、以及 missing `harbor.core.module_capsule.build_module_card_frontmatter`。
- 结论：4 个 blocker 均判定为 `expected_baseline_change`，未发现会破坏默认行为、CI mode 语义、JSON 稳定性、write target 安全边界的 `real_blocker`。
- missing function 判定：`build_module_card_frontmatter` 已被 `context_integrity` + `compose_markdown_with_frontmatter` + `generate_module_card` 链路替代；源码/测试零引用；非 public API。
- 最小修正：移除过期文档引用（`harbor/core/README.md`、`harbor/README.md` 中的 `build_module_card_frontmatter` 行），未改功能语义。
- `harbor checkpoint --ci --format json` 当前仍 exit 1，根因是 baseline 未 accept 的预期漂移集合（summary: `missing=2`, `drift=26`, `untracked=130`, `ddt_failures=0`, `confirmed_contract_impact=0`）。

## Reviewed Items

### harbor.cli.main.main
- why flagged: baseline 记录为 `possible_semantic_drift`（`body changed, contract static`），且属于 public CLI 路径。
- behavior: 新增/强化 CI gate 与安全输出路径（`checkpoint/stale/doctor --ci`、JSON 单对象输出、`writes_files=false`、checkpoint `--format` 仅 CI 模式可用）；不改变非 CI checkpoint 的默认语义。
- contracts/docs: 函数 docstring 已覆盖 workflow facade 与异常语义；README/README.en/RELEASE 已覆盖 CI mode 语义与 gate 行为。
- tests: `tests/test_checkpoint_ci.py`、`tests/test_ci_mode.py`、`tests/test_cli_v2.py`、`tests/test_cli_help_and_ux.py` 覆盖 checkpoint/stale/doctor CI 行为、JSON 输出、no-write 回归、帮助文案。
- docs/release: 已覆盖（README、README.en、RELEASE 均有 `checkpoint --ci`/`stale --ci`/`doctor --ci` 语义说明）。
- public impact: 影响 public CLI/JSON（预期 P0/P1 目标）；未发现 path leak、JSON 混杂文本、exit code 语义回退。
- classification: `expected_baseline_change`
- action: 保持现状，无功能修补。

### harbor.core.l2.L2Generator.write
- why flagged: baseline 记录为 `possible_semantic_drift`，归因写入行为变化。
- behavior: 写 canonical ` .harbor/views/l2/<module>/README.md`（含 frontmatter），并按配置可选写 export `<module>/README.md`（纯 body）；写入 `_meta.json`；增加 module path traversal 与 repo-root 逃逸防护。
- contracts/docs: 代码行为与 README/RELEASE 中 canonical-first + export 可选策略一致；无反向冲突迹象。
- tests: `tests/test_l2_paths.py` 覆盖 canonical+export 默认行为、禁用 export、legacy meta 兼容、路径越界拒绝、重复写稳定性。
- docs/release: 已覆盖 canonical 路径策略与 CI/read-only 语义关联说明。
- public impact: 影响 write target 与 generated view 格式（预期）；未发现写错目标或绝对路径泄漏。
- classification: `expected_baseline_change`
- action: 保持现状，无功能修补。

### harbor.core.module_capsule.write_module_capsule
- why flagged: baseline 记录为 `possible_semantic_drift`，归因生成/写入链路重构。
- behavior: 统一由 `preview_module_capsule` 生成 3 个视图并写入 canonical；按配置可选写 docs export；`module-card.md` frontmatter 注入 `view_fingerprint/fingerprint`，stale 对比改为读 frontmatter 指纹。
- contracts/docs: 与当前 context-integrity 方案一致；无保留旧 helper 的必要合同证据。
- tests: `tests/test_module_capsule.py`、`tests/test_module_capsule_stale.py` 覆盖三文件写入、frontmatter 指纹、stale 判断、路径安全、fingerprint 语义。
- docs/release: module capsule 相关文档与命令说明已覆盖。
- public impact: 影响 generated view format / write target（预期）；未发现 canonical/export 混写错误。
- classification: `expected_baseline_change`
- action: 保持现状，无功能修补。

### harbor.core.project_structure.write_project_structure
- why flagged: baseline 记录为 `possible_semantic_drift`，归因写入目标和渲染细节变化。
- behavior: canonical 写 `.harbor/views/project-structure.md`（frontmatter + body）；docs export 仅在配置启用时写入；默认不覆盖 legacy docs 导出。
- contracts/docs: 与 v1.3.0 canonical workspace 规则一致；CLI preview/write 行为与帮助文案一致。
- tests: `tests/test_project_structure.py`、`tests/test_cli_project_structure.py` 覆盖 preview 不落盘、canonical-first、repeat-write 稳定、export 可选、legacy 不误覆盖、副作用隔离。
- docs/release: README/RELEASE 包含 `harbor project structure --write` 与 canonical 路径说明。
- public impact: 影响 generated view format / write target（预期）；未发现 write target 错误。
- classification: `expected_baseline_change`
- action: 保持现状，无功能修补。

### missing: harbor.core.module_capsule.build_module_card_frontmatter
- 是否仍被源码/测试引用: 否（全仓 grep 无源码/测试引用）。
- 是否 public API: 否（内部 helper，不在 CLI/public contract 面向层）。
- 是否被替代: 是；frontmatter 逻辑已由 `context_integrity` + `compose_markdown_with_frontmatter` + `generate_module_card` / `write_module_capsule` 链路承担。
- 新 frontmatter 行为测试: 有（`test_write_module_card_contains_frontmatter_fingerprint`、`test_capsule_stale_uses_view_fingerprint_not_source_fingerprint`、`test_context_integrity.py` 相关用例）。
- 兼容 wrapper 需求: 无（无调用方）。
- 附加修正: 已移除 `harbor/core/README.md` 与 `harbor/README.md` 中过时文档引用。
- classification: `expected_baseline_change`
- action: 不保留 wrapper；维持现状。

## Real Blockers
- 无。

## Needs Human Review
- 无阻断项；仅建议 accept 前执行一次人工 spot-check（见下方 Accept Recommendation）。

## Expected Baseline Changes
- `harbor.cli.main.main`（public CLI CI gate 路径扩展）。
- `harbor.core.l2.L2Generator.write`（canonical/export + frontmatter 写入策略）。
- `harbor.core.module_capsule.write_module_capsule`（frontmatter/fingerprint 与导出策略）。
- `harbor.core.project_structure.write_project_structure`（canonical-first + export 可选）。
- `harbor.core.module_capsule.build_module_card_frontmatter` missing（重构替代，且无引用）。

## Tests
- targeted:
  - `pytest tests/test_checkpoint_ci.py` -> `12 passed`
  - `pytest tests/test_ci_mode.py` -> `10 passed`
  - `pytest tests/test_context_integrity.py` -> `7 passed`
  - `pytest tests/test_module_capsule.py tests/test_module_capsule_stale.py` -> `18 passed`
  - `pytest tests/test_l2_paths.py tests/test_cli_docs_modes.py` -> `28 passed`
  - `pytest tests/test_project_structure.py tests/test_cli_project_structure.py` -> `15 passed`
  - `pytest tests/test_cli_v2.py tests/test_cli_help_and_ux.py` -> `26 passed`
- full pytest:
  - `pytest` -> `333 passed`

## Harbor Checks
- stale --ci:
  - `harbor stale --ci` -> `PASS (exit 0)`, `writes_files: false`
- doctor --ci:
  - `harbor doctor --ci` -> `PASS (exit 0)`, `writes_files: false`，含 advisory（changed records）
- checkpoint --ci json:
  - `harbor checkpoint --ci --format json` -> `FAIL (exit 1)`, `writes_files: false`
  - summary: `missing=2`, `drift=26`, `untracked=130`, `ddt_failures=0`, `confirmed_contract_impact=0`
  - 判定：本次 exit 1 来源于 baseline 漂移集合（expected baseline changes + 既有未接收基线），未发现新增 real blocker 证据。

## Accept Recommendation
- accept / do not accept:
  - `accept（建议在人工抽查后执行）`
- reason:
  - 本轮 4 个 blocker 与 missing function 已复核为 `expected_baseline_change`，测试与 CI 语义一致，未发现 real blocker。
- required manual checks before accept:
  - 抽查 `harbor checkpoint --ci --format json` 输出中 `ci_failures/advisory/summary` 键稳定性与无绝对路径泄漏。
  - 抽查 `docs --write / module seal --write / project structure --write` 的 canonical-first 写入顺序与目标路径。
  - 抽查 `stale --ci` / `doctor --ci` 仍为只读 gate，且阻断条件未回退。
