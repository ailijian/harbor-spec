/**
 * Return the incremented input value for an internal helper demo.
 *
 * @param x Input value.
 * @returns Incremented value.
 */
function helper(x: number): number {
  return x + 1;
}

export { helper };
